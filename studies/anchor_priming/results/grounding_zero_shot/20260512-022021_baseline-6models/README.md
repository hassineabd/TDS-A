# Baseline run — DO NOT DELETE

This directory holds the first benchmark run used to draft the
companion TDS article. It is referenced verbatim in:
  - The study's article_draft.md
  - The cross-study comparison in anchor_priming/README.md

If you want to re-run the benchmark, do so in a NEW timestamped
directory (the runner does this by default). Leave this one as the
frozen reference.

Run on: 2026-05-25 17:33:43 CEST
Rows:   144
